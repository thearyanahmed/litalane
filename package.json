{
  "name": "litalane",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --bun vite dev",
    "build": "bun --bun vite build",
    "preview": "bun --bun vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "optimize:images": "bash scripts/optimize-images.sh all && node scripts/process.mjs",
    "optimize:food": "bash scripts/optimize-images.sh food && node scripts/process.mjs food",
    "optimize:care": "bash scripts/optimize-images.sh care && node scripts/process.mjs care",
    "optimize:home": "bash scripts/optimize-images.sh home && node scripts/process.mjs home",
    "process": "node scripts/process.mjs"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^6",
    "sharp": "^0.34.5",
    "smartcrop-sharp": "^2.0.8",
    "svelte": "^5.1.0",
    "svelte-check": "^4.0.0",
    "vite": "^7"
  }
}
